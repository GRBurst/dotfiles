#!/usr/bin/env bash

set -Eeuo pipefail

SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )"

source "$SCRIPT_DIR/dotfiles-env"

git -c status.showUntrackedFiles=no -c core.excludesfile="$DOTIFLES_EXCLUDESFILE" $@
